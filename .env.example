# Local demo: leave provider and admin credentials empty.
# With no ADMIN_TOKEN, admin APIs are open. Bind locally; configure a strong token before hosting.
GEMINI_API_KEY=
ADMIN_TOKEN=
STORAGE_DIR=./data
RATE_LIMIT_PER_MINUTE=60
